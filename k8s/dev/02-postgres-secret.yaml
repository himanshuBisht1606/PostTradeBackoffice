# NOTE: This file is a placeholder template.
# Actual secret values are injected by the pipeline (deploy-dev.yml step 1.8)
# using: kubectl create secret generic --dry-run=client -o yaml | kubectl apply -f -
# Do NOT commit real base64-encoded passwords into this file.
apiVersion: v1
kind: Secret
metadata:
  name: posttrade-dev-db-secret
  namespace: posttrade-dev
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
  ConnectionStrings__DefaultConnection: "Host=postgres-service;Port=5432;Database=posttrade_dev;Username=posttrade_dev_user;Password=REPLACE_ME"
