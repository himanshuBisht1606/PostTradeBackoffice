# NOTE: This file is a placeholder template.
# Actual secret values are injected by the pipeline (deploy-qa.yml step 3.8)
# using: kubectl create secret generic --dry-run=client -o yaml | kubectl apply -f -
# Do NOT commit real base64-encoded passwords into this file.
apiVersion: v1
kind: Secret
metadata:
  name: posttrade-qa-db-secret
  namespace: posttrade-qa
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
  ConnectionStrings__DefaultConnection: "Host=postgres-service;Port=5432;Database=posttrade_qa;Username=posttrade_qa_user;Password=REPLACE_ME"
